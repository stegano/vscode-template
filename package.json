{
  "name": "lite-template",
  "displayName": "lite-template",
  "description": "This extension templates the file folder structure, making it easy to reuse.",
  "version": "0.0.1",
  "publisher": "yongwoo",
  "engines": {
    "vscode": "^1.40.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/stegano/vscode-lite-template.git"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.createNew",
    "onCommand:extension.createNewWithRename"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "extension.createNew",
        "title": "Lite-Template: Create new"
      },
      {
        "command": "extension.createNewWithRename",
        "title": "Lite-Template: Create new (with rename)"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "extension.createNew",
          "group": "navigation"
        },
        {
          "command": "extension.createNewWithRename",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "extension.createNew",
        "key": "ctrl+alt+t",
        "mac": "cmd+alt+t",
        "when": "editorTextFocus"
      },
      {
        "command": "extension.createNewWithRename",
        "key": "ctrl+shift+t",
        "mac": "cmd+shift+t",
        "when": "editorTextFocus"
      }
    ]
  },
  "scripts": {
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.1.1",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.11.7",
    "@types/vscode": "^1.40.0",
    "eslint": "^6.6.0",
    "glob": "^7.1.5",
    "mocha": "^6.2.2",
    "typescript": "^3.6.4",
    "vscode-test": "^1.2.2"
  },
  "dependencies": {
    "change-case": "^3.1.0",
    "fs-extra": "^8.1.0",
    "js-beautify": "^1.10.2"
  }
}
